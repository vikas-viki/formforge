export const CONSTANTS = {
    server: {
        cookies: {
            SESSION_TOKEN: "session_token"
        },
        headers: {
            userId: 'x-user-id'
        }
    }
}

export const SALT_ROUNDS = 10;

export const USER_TYPES = {
    user: "USER",
    admin: "ADMIN"
}